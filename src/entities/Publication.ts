export class Publication {
    constructor(public title: string, public date: string, public userName: String) {
        this.title = title;
        this.date = date;
        this.userName = userName;
    }
}